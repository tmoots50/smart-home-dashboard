.env
.env.*
!.env.example
_secrets/
.envrc.local
*.pem
*.key
credentials.json
service-account*.json
.DS_Store
node_modules/
dist/
build/
__pycache__/
*.pyc
venv/
.venv/
*.log
.vscode/
.idea/
.claude-worktrees/

# Vite app
app/node_modules/
app/dist/
app/.vite/

# Claude Code — personal local overlays (project-level skills + settings stay tracked)
.claude/settings.local.json

# Vite local env (Google OAuth + dashboard token)
app/.env.local

# Wrangler dev secrets
app/.dev.vars

# QA harness output (screenshots for review, playwright traces) — never committed
app/tests/qa/artifacts/
app/test-results/
app/playwright-report/
